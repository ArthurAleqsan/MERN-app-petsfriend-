import React, {Component} from 'react';

export default class Partners extends Component {
    render() {
        return (
            <div className="main-partners">
                inside partners component
            </div>
        )
    }
}